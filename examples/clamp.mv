mod math

pure fn clamp(x: i32, lo: i32, hi: i32) -> i32
    requires (lo <= hi)
    ensures ((result >= lo) and (result <= hi))
{
    if (x < lo) {
        lo
    } else if (x > hi) {
        hi
    } else {
        x
    }
}
