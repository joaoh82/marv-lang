mod demo

pure fn ops(a: i64, b: i64) -> i64 {
    let sum = a + b
    let diff = a - b
    let prod = a * b
    let quot = a / b
    let rem = a % b
    if a >= b {
        sum + diff + prod + quot + rem
    } else {
        sum - diff
    }
}
